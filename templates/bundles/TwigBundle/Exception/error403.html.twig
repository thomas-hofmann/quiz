{% include ('base.html.twig') %}
<div class="container">
    <div class="box box--70 border rounded shadow p-3 mx-auto">
        <h1>Zugriff verweigert (403)</h1>
        <p>Sie haben keine Berechtigung, auf diese Seite zuzugreifen.</p>
        {% if exception is defined and exception.message is defined %}
            <strong>Fehlermeldung</strong>
            <p class="text-danger">{{ exception.message }}</p>
        {% endif %}
        <a href="{{ path('home') }}" type="button" class="btn btn-dark mt-2"><i class="fa-solid fa-arrow-left-long"></i> Zurück zur Startseite</a>
    </div>
</div>
{% include ('footer.html.twig') %}
