{% include ('base.html.twig') %}
<div class="container">
    <h2>Rangliste</h2>
    <table class="table table-striped table-dark box">
        <thead>
            <tr>
              <th scope="col">#</th>
              <th scope="col">Matrikelnummer</th>
              <th scope="col">Richtige Antworten</th>
            </tr>
        </thead>
        <tbody>
            {% for entry in leaderBoardEntries %}
                <tr>
                    <td>{{ loop.index }}</td>
                    <td>{% if loop.index == 1 %}<i class="fa-solid fa-crown"></i>{% endif %} {{ entry.matrikelnumber }} {% if loop.index == 1 %}<i class="fa-solid fa-crown"></i>{% endif %}</td>
                    <td>{{ entry.score }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    <a href="/dashboard" type="button" class="btn btn-primary mt-2">Zurück zum Dashboard</a>
</div>
{% include ('footer.html.twig') %}